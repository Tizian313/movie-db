<UserControl x:Class="WPF_MovieDb.Views.MovieEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MovieDb.Views"

             xmlns:viewModel="clr-namespace:WPF_MovieDb.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1600"
             d:DesignHeight="900">

    <Grid>
        <!-- Here to act as event trigger -->
        <Label Content="{Binding OnChangedToView}" Visibility="Hidden"/>
        <!-- Here to act as event trigger -->


        <!-- UI Elements -->
        <Label Content="{Binding TitleText}" Style="{StaticResource TitelLabel}" Height="150" Grid.ColumnSpan="2"/>
        
        <Label Content="Name: " Style="{StaticResource InfoLabel}" HorizontalAlignment="Right" Grid.Row="1"/>
        <TextBox Text="{Binding NameInput, UpdateSourceTrigger=PropertyChanged}" FontSize="48" Margin="10,0,10,0" Height="80" Grid.Column="1" Grid.Row="1"/>

        <Label Content="Genres: " Style="{StaticResource InfoLabel}" HorizontalAlignment="Right"               Grid.Row="2"/>
        <TextBox Text="{Binding GenresInput, UpdateSourceTrigger=PropertyChanged}" FontSize="48" Margin="10,0,120,0" Height="80" Grid.Row="2" Grid.Column="1"/>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" Margin="0, 0, 10, -50" Grid.Row="2" Grid.Column="1">
            <ComboBox ItemsSource="{Binding GenreItemsSource}" Height="30" SelectedItem="{Binding SelectedGenre}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CanvasText}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <Button Content="Add selection" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Height="40" Margin="0, 0, 10, 40" Grid.Row="2" Width="100" FontSize="16" Command="{Binding AddGenre}" Grid.Column="2"/>

        <Label Content="Directors: " Style="{StaticResource InfoLabel}" HorizontalAlignment="Right"            Grid.Row="3"/>
        <TextBox Text="{Binding DirectorInput, UpdateSourceTrigger=PropertyChanged}" FontSize="48" Margin="10,0,120,0" Height="80"                Grid.Row="3" Grid.Column="1"/>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" Margin="0, 0, 10, -50" Grid.Row="3" Grid.Column="1">
            <ComboBox ItemsSource="{Binding PersonItemsSource}" Height="30" SelectedItem="{Binding SelectedDirector}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CanvasText}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <Button Content="Add selection" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Height="40" Margin="0, 0, 10, 40" Grid.Row="3" Width="100" FontSize="16" Command="{Binding AddDirector}" Grid.Column="1"/>

        <Label Content="Actors: " Style="{StaticResource InfoLabel}" HorizontalAlignment="Right" Grid.Row="4"/>
        <TextBox Text="{Binding ActorInput, UpdateSourceTrigger=PropertyChanged}" FontSize="48" Margin="10,0,120,0" Height="80"      Grid.Row="4" Grid.Column="1"/>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" Margin="0, 0, 10, -50" Grid.Row="4" Grid.Column="1">
            <ComboBox ItemsSource="{Binding PersonItemsSource}" Height="30" SelectedItem="{Binding SelectedActor}" Grid.Column="1">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CanvasText}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <Button Content="Add selection" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Height="40" Margin="0, 0, 10, 40" Grid.Row="4" Width="100" FontSize="16" Command="{Binding AddActor}" Grid.Column="1"/>

        <Label Content="Rating: " Style="{StaticResource InfoLabel}" HorizontalAlignment="Right" Grid.Row="5" Height="90"/>
        <Canvas Width="400" Height="100" Margin="10" Grid.Row="5" Grid.Column="1" HorizontalAlignment="Left">
            <Image Source="../Resources/emptyStars.png" Height="80" Width="400" Stretch="UniformToFill"/>
            <Image Source="../Resources/filledStars.png" Height="80" Width="{Binding StarWidth}" Stretch="UniformToFill"/>
            <Slider Maximum="100" Value="{Binding SliderValue}" TickFrequency="1" IsSnapToTickEnabled="True" Width="400" Margin="0,90"/>
        </Canvas>
        <Label Content="{Binding RatingText}" Style="{StaticResource InfoLabel}" HorizontalAlignment="Left" Grid.Row="5" Grid.Column="1" Height="90" Margin="440, 0"/>

        <Button Style="{StaticResource IconButton}" Grid.Row="9" Command="{Binding Cancel}" Margin="10" IsCancel="True" ToolTip="Cancel" HorizontalAlignment="Left" VerticalAlignment="Bottom" >
            <StackPanel>
                <Image Source="../Resources/return.png"/>
            </StackPanel>
        </Button>

        <Button Style="{StaticResource IconButton}" Grid.Row="9" Command="{Binding Enter}" Margin="120, 10" IsDefault="True" ToolTip="Save" HorizontalAlignment="Left" VerticalAlignment="Bottom" Grid.ColumnSpan="2">
            <StackPanel>
                <Image Source="../Resources/diskette.png"/>
            </StackPanel>
        </Button>
        <Image Style="{StaticResource CornerLogo}" Grid.Row="9" Grid.Column="9"/>
        <!-- UI Elements -->

        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="330"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>

    