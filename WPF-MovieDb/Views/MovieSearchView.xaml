<UserControl x:Class="WPF_MovieDb.Views.MovieSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MovieDb.Views"

             xmlns:viewModel="clr-namespace:WPF_MovieDb.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1600"
             d:DesignHeight="900">

    <Grid>
        <!-- Here to act as event trigger -->
        <Label Content="{Binding OnChangedToView}" Visibility="Hidden"/>
        <!-- Here to act as event trigger -->


        <!-- ↓ UI Elements ↓ -->

        <!-- "Search by" UI -->
        <TextBox Text="{Binding SearchBarInput, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" FontSize="48" Height="80" Width="1490" Margin="10,0,10,0" HorizontalAlignment="Left"/>

        <Label Content="Search by                    :" Style="{StaticResource InfoLabel}" HorizontalAlignment="Right" VerticalAlignment="Center" Height="60" FontSize="40" VerticalContentAlignment="Bottom"/>

        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20, 0">
            <ComboBox ItemsSource="{Binding SearchBySource}" Height="50" Width="200" SelectedItem="{Binding SelectedSearchBy}" IsSynchronizedWithCurrentItem="True">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CanvasText}" FontSize="30"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <!-- "Search by" UI -->

        <!-- Ranking UI -->
        <Label Content="Ranked" Style="{StaticResource InfoLabel}" HorizontalAlignment="Left" Height="80" FontSize="40" Margin="10, -10, 0, 0" Grid.Row="1"/>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10, 50, 0, 0" Grid.Row="1">
            <ComboBox ItemsSource="{Binding RankBySource}" Height="50" Width="210" SelectedItem="{Binding SelectedRankBy}" IsSynchronizedWithCurrentItem="True">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CanvasText}" FontSize="30"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <Button Command="{Binding ChangeOrder}" Style="{StaticResource IconButton}" HorizontalAlignment="Left" Margin="230, 0" ToolTip="Change the order in which result are is displayed." Grid.ColumnSpan="2" Grid.Row="1">
            <StackPanel>
                <Image Source="../Resources/swap.png"/>
            </StackPanel>
        </Button>
        <!-- Ranking UI -->

        <!-- Genre search UI -->
        <Label Content="Excluded genres" Style="{StaticResource InfoLabel}" HorizontalAlignment="Left" Height="80" FontSize="40" Margin="10, -240, 0, 0" Grid.Row="2"/>

        <Border BorderThickness="2" BorderBrush="LightGray" Width="210" Height="210" Grid.Row="2" Margin="10, 0, 0, 0" HorizontalAlignment="Left">
            <ScrollViewer>
                <Canvas VerticalAlignment="Top" Height="560">
                    <ItemsControl ItemsSource="{Binding GenreSelectionSource}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Viewbox Height="25">
                                        <CheckBox IsChecked="{Binding IsChecked}" Command="{Binding OnToggleCommand}" Width="25"/>
                                    </Viewbox>
                                    <TextBlock Text="{Binding Text}" FontSize="20" Foreground="#F29224"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </ScrollViewer>
        </Border>

        <Button Command="{Binding ChangeAllBoxesCommand}" CommandParameter="True" Style="{StaticResource IconButton}" ToolTip="Select all genres." Grid.Row="2" Margin="230, -105, 0, 0" Grid.ColumnSpan="2" HorizontalAlignment="Left">
            <StackPanel>
                <Image Source="../Resources/selectedBoxes.png"/>
            </StackPanel>
        </Button>

        <Button Command="{Binding ChangeAllBoxesCommand}" CommandParameter="False" Style="{StaticResource IconButton}" ToolTip="Deselect all genres." Grid.Row="2" Grid.ColumnSpan="2" Margin="230, 105, 0, 0"  HorizontalAlignment="Left">
            <StackPanel>
                <Image Source="../Resources/deselectedBoxes.png"/>
            </StackPanel>
        </Button>
        <!-- Genre search UI -->

        <!-- Scrollable movie display -->
        <Border Grid.Row="1" Background="#FF2B2B2B" Grid.Column="1" Margin="10, 10, 10, 10" Grid.RowSpan="2" BorderThickness="1" BorderBrush="White">
            <ScrollViewer>
                <Canvas Height="{Binding HeightOfCanvas}" VerticalAlignment="Top">
                    <ItemsControl ItemsSource="{Binding MovieItemSource}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="White" BorderThickness="0.3" >
                                    <Button Width="1490" Command="{Binding CanvasCommand}" Style="{StaticResource MovieButton}" CommandParameter="{Binding CanvasButtonParameter}">
                                        <StackPanel Orientation="Horizontal">
                                            <Canvas Width="260" Height="80">
                                                <Image Source="../Resources/emptyStars.png" Height="50" Width="250" Stretch="UniformToFill" Margin="5, 15"/>
                                                <Image Source="../Resources/filledStars.png" Height="50" Width="{Binding StarWidth}" Stretch="UniformToFill" Margin="5, 15"/>
                                            </Canvas>
                                            <Label Content="{Binding CanvasButtonText}" Style="{StaticResource InfoLabel}" FontSize="60"/>
                                        </StackPanel>
                                    </Button>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </ScrollViewer>
        </Border>
        <!-- Scrollable movie display -->

        <!-- Bottom UI -->

        <Button CommandParameter="Return" Grid.Row="9" Command="{Binding Return}" IsCancel="True" Style="{StaticResource IconButton}" HorizontalAlignment="Left" Margin="10,10,0,10" ToolTip="Return">
            <StackPanel>
                <Image Source="../Resources/return.png" />
            </StackPanel>
        </Button>
        <!-- Bottom UI -->

        <!-- ↑ UI Elements ↑ -->


        <Grid.RowDefinitions>
            <RowDefinition Height="120" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="410"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>
    