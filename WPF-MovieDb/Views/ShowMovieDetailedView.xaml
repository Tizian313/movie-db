<UserControl x:Class="WPF_MovieDb.Views.ShowMovieDetailedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MovieDb.Views"
             xmlns:viewModels="clr-namespace:WPF_MovieDb.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1600"
             d:DesignHeight="900">

    <Grid>

        <!-- UI Elements -->
        <Label Content="{Binding MovieName}"   Style="{StaticResource TitelLabel}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="2" VerticalAlignment="Top" Height="160" HorizontalAlignment="Left" Margin="30, 0"/>

        <Canvas Width="260" Height="80" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50, 10">
            <Image Source="../Resources/emptyStars.png" Height="80" Width="400" Stretch="UniformToFill"/>
            <Image Source="../Resources/filledStars.png" Height="80" Width="{Binding StarWidth}" Stretch="UniformToFill"/>
        </Canvas>

        <Label Content="{Binding CreatorLabel}" Style="{StaticResource InfoLabel}" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="30" Padding="50,20"/>

        <Label Content="Actors"                 Style="{StaticResource InfoLabel}" Grid.Column="0" Grid.Row="1" VerticalAlignment="Bottom"/>
        <Label Content="Directors"              Style="{StaticResource InfoLabel}" Grid.Column="1" Grid.Row="1" VerticalAlignment="Bottom"/>
        <Label Content="Genres"                 Style="{StaticResource InfoLabel}" Grid.Column="2" Grid.Row="1" VerticalAlignment="Bottom"/>

        
        <ScrollViewer Grid.Row="2" Background="#FF2B2B2B" Margin="10, 10, 10, 10">
            <Canvas Height="{Binding ActorCanvasHeight}" VerticalAlignment="Top">
                <ItemsControl ItemsSource="{Binding ActorItemsSource}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding CanvasText}" Style="{StaticResource InfoLabel}" FontSize="50" Height="70" HorizontalAlignment="Left"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>
        
        <ScrollViewer Grid.Row="2" Grid.Column="1" Background="#FF2B2B2B" Margin="10, 10, 10, 10">
            <Canvas Height="{Binding DirectorCanvasHeight}" VerticalAlignment="Top">
                <ItemsControl ItemsSource="{Binding DirectorItemsSource}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding CanvasText}" Style="{StaticResource InfoLabel}" FontSize="50" Height="70" HorizontalAlignment="Left"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>
        
        <ScrollViewer Grid.Row="2" Grid.Column="2" Background="#FF2B2B2B" Margin="10, 10, 10, 10">
            <Canvas Height="{Binding GenreCanvasHeight}" VerticalAlignment="Top">
                <ItemsControl ItemsSource="{Binding GenreItemsSource}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding CanvasText}" Style="{StaticResource InfoLabel}" FontSize="50" Height="70" HorizontalAlignment="Left"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>

        
        <Button Style="{StaticResource IconButton}" ToolTip="Back" Grid.Row="9" Command="{Binding Back}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10" IsCancel="True">
            <StackPanel>
                <Image Source="../Resources/return.png"/>
            </StackPanel>
        </Button>

        <Button Style="{StaticResource IconButton}" ToolTip="Edit" Grid.Row="9" Command="{Binding Edit}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="120, 10" >
            <StackPanel>
                <Image Source="../Resources/editing.png"/>
            </StackPanel>
        </Button>

        <Button Style="{StaticResource IconButton}" ToolTip="Delete" Grid.Row="9" Command="{Binding Delete}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="230, 10" Grid.ColumnSpan="2">
            <StackPanel>
                <Image Source="../Resources/bin.png"/>
            </StackPanel>
        </Button>
        
        <Image                     Grid.Row="9" Grid.Column="9" Style="{StaticResource CornerLogo}"/>
        <!-- UI Elements -->

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="160" />
            <RowDefinition Height="240" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>
    </Grid>

</UserControl>